# Generate unique tree pairs

Creates all unique pairwise combinations of tree indices for overlap
analysis.

## Usage

``` r
make_tree_pairs(n)
```

## Arguments

- n:

  Number of trees.

## Value

A 2-row matrix where each column is a pair of indices.
